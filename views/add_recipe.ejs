<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adicionar receita</title>
</head>
<body>
    <h1>Adicionar uma nova receita</h1>
    <form method="post" action="/add">
        <label class="titulo" for="title">Título</label>
        <input class="titulo" type="text" id="title" name="title" required>
        <br>
        <label class="titulo" for="description">Ingredientes</label>
        <textarea class="descricao" id="description" name="description" required></textarea>
        <br>
        <label class="titulo" for="password">Senha</label>
        <input class="titulo" type="password" id="password" name="password" required>
        <br>
        <button class="btn" type="submit">Adicionar</button>
    </form>
    <a href="/">Voltar</a>

    <style>
        @import url('https://fonts.googleapis.com/css?family=Work+Sans:400,600');
        body {
            background-color: #232D3F;
            font-weight: 550;
            font-family: 'Work Sans', sans-serif;
            padding: 20px;
        }
        
        .titulo, .descricao {
            color: white;
            margin: 5px;
            padding: 8px;
            background-color: #2E3B4E;
            border: none;
            border-radius: 5px;
            display: block;
            width: 25%;
            box-sizing: border-box;
        }

        .descricao {
            min-height: 100px;
        }

        .btn {
            margin: 5px 0;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn:hover {
            background-color: #45a049;
        }

        h1 {
            color: whitesmoke;
        }

        a {
            color: whitesmoke;
            text-decoration: none;
            display: inline-block;
            margin-top: 10px;
        }

        a:hover {
            text-decoration: underline;
        }

        @media (max-width: 600px) {
            .titulo, .descricao {
                width: 100%;
            }

            ul, li {
                width: 90%;
            }
        }
    </style>
</body>
</html>
